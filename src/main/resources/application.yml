server:
  port: 8081

spring:
  application:
    name: bi-dashboard-api

  # SQL Server Configuration (Primary DataSource)
  datasource:
    url: jdbc:sqlserver://10.9.182.73:1433;databaseName=EmployeeDB;encrypt=true;trustServerCertificate=true
    username: Sa
    password: P@55w0rd@sql
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect
        format_sql: true

  cache:
    type: simple
    cache-names: analytics, kpis, salesTrends, topProducts, customerSegments, geographicSales

# Snowflake Configuration (Secondary DataSource)
snowflake:
  url: jdbc:snowflake://ILNUYKT-SH44292.snowflakecomputing.com
  username: JSASANAM
  password: JamesBond@7117
  warehouse: COMPUTE_WH
  database: ORDER_MANAGEMENT_DB
  schema: SALES_SCHEMA
  jdbc-query-result-format: json

# JWT Configuration
jwt:
  secret: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
  expiration: 86400000
  refresh-expiration: 604800000

# SpringDoc OpenAPI / Swagger Configuration
springdoc:
  api-docs:
    path: /v3/api-docs
    enabled: true
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    operations-sorter: method
    tags-sorter: alpha
    doc-expansion: none
    default-models-expand-depth: 3
    display-request-duration: true
  show-actuator: false

# Logging Configuration (managed by logback-spring.xml)